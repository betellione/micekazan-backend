@model Scanners

@using WebApp1.Views.Event
@{
    Layout = "_Layout";
    ViewData["ActivePage"] = EventNavPages.Tickets;
}

<div class="tabcontent">
    <div class="content_block">
        <table class="details">
            <tr>
                <th><p>Логин</p></td>
                <th></td>
            </tr>
            
            @if (!Model.ScannersList.Any())
            {
                <tr><td colspan="99">К этому мероприятию пока не добавлены билетеры</td></tr>
            }
            else
            {
                @foreach (var scanner in Model.ScannersList) {
                    <tr>
                        <td>
                            <p hidden>@scanner.Id</p>
                            <p>@scanner.Username</p>
                        </td>
                        <td class="custom-link-btm">
                            <p onclick="location.href='@Url.Action("EditScanner", new { userId = scanner.Id, eventId = Model.EventId, eventName = Model.EventName })';">подробнее</p>
                        </td>
                    </tr>
                }            
            }
        </table>        
    </div>

    <div class="content_bottom">
        <div class="custom-btm">
            <button asp-controller="Event"
                asp-action="AddScanner"
                asp-route-eventId="@Model.EventId"
                asp-route-eventName="@Model.EventName">
                Добавить билетера
            </button>
        </div>
    </div>
</div>

<!--
<div class="tabcontent">
    <div class="content_block">
        @if (!Model.ScannersList.Any())
        {
            <p>К этому мероприятию пока не добавлены билетеры</p>
        }
        else
        {
            <div class="table-responsive custom-table mb-3">
                <table class="table table-dark table-hover align-middle">
                    <thead>
                    <th>
                        <div class="table-col-header">Логин</div>
                    </th>
                    </thead>
                    <tbody>
                    @foreach (var scanner in Model.ScannersList)
                    {
                        <tr onclick="location.href='@Url.Action("EditScanner", new { userId = scanner.Id, eventId = Model.EventId, eventName = Model.EventName })';">
                            <td class="table-row">
                                <div hidden>@scanner.Id</div>
                                <div class="table-col">@scanner.Username</div>
                            </td>
                        </tr>
                    }
                    </tbody>
                </table>
            </div>
        }
    </div>

    <a asp-controller="Event"
        asp-action="AddScanner"
        asp-route-eventId="@Model.EventId"
        asp-route-eventName="@Model.EventName"
        class="w-100 btn btn-lg btn-warning">
        Добавить билетера
    </a>
</div>
-->